#  TIC、XIC とスペクトル間の移動

この章は、[mzML ファイルを開く](02_Opening_mzML_ja.md)の手順で mzML ファイルを開いて
いることを前提にしています。

##  TIC

TIC（トータルイオンカレント）クロマトグラムは、MS スペクトルの強度の合計を保持時間
に沿って表したものです。測定中のどの時間にイオンが検出されたかを、全体として把握
できます。

- mzML ファイルに TIC などのクロマトグラムが含まれている場合は、それが
  **Chromatogram** テーブルに表示されます。
- ファイルにクロマトグラムが含まれていない場合は、Mass++4 が MS スペクトルから TIC
  を計算し、**TIC** として表示します。

**Chromatogram** テーブルの行をクリックすると、**Profile** タブの上段に描画されます。

##  XIC

XIC（抽出イオンクロマトグラム）は、1 つの m/z の強度を保持時間に沿って表したもの
です。特定のイオンがいつ検出されたかを確認するのに使います。

1. 目的のイオンを含むスペクトルを描画します。
2. スペクトル上のピークを右クリックし、**XIC ...** を選びます。

   ![スペクトルの右クリックメニュー](images/screenshots/03-01-xic_menu.png)

3. **XIC** ダイアログで値を確認し、必要なら編集して **OK** をクリックします。

   ![XIC ダイアログ](images/screenshots/03-02-xic_dialog.png)

   | 項目 | 意味 | 初期値 |
   | --- | --- | --- |
   | `m/z` | m/z の範囲の中心 | 右クリックした位置の m/z |
   | `tol.` | m/z の範囲の片側の幅 | `0.1` |

XIC は `XIC [mz=<m/z>]` として **Chromatogram** テーブルに追加され、上段に描画され
ます。各 MS スペクトルについて、`m/z` ± `tol.` の範囲の強度が合計されます。MS/MS
スペクトルは使われません。

![Chromatogram テーブルに追加されて描画された XIC](images/screenshots/03-03-xic_result.png)

XIC はファイルを開いている間 **Chromatogram** テーブルに残るので、複数の XIC を作って
テーブルで切り替えられます。

##  スペクトル間の移動

###  Spectrum テーブルを使う

**Spectrum** テーブルの行をクリックすると、そのスペクトルが描画されます。各行の名前
には、`Scan 8416 [40.5137]` のようにスキャン番号と保持時間が含まれています。

行をクリックした後は、キーボードの **↑** と **↓** キーで前後のスペクトルに移動
できます。グラフも選択に合わせて切り替わります。

###  クロマトグラムから開く

XIC のピークなど、クロマトグラム上の位置にあたるスペクトルを開けます。

1. クロマトグラム上の目的の保持時間の位置で右クリックし、**Spectrum...** を選び
   ます。

   ![クロマトグラムの右クリックメニュー](images/screenshots/03-04-spectrum_menu.png)

2. **Spectrum** ダイアログで値を設定し、**OK** をクリックします。

   ![Spectrum ダイアログ](images/screenshots/03-05-spectrum_dialog.png)

   | 項目 | 意味 | 初期値 |
   | --- | --- | --- |
   | `RT Range` | 表示するスペクトルの保持時間の範囲 | 右クリックした位置の保持時間（開始と終了の両方） |
   | `Target` | `MS`、`MS/MS`、`All` のどのスペクトルを対象にするか | `MS` |

描画されるスペクトルは、範囲内にある対象スペクトルの本数によって変わります。

| 範囲内のスペクトル | 結果 |
| --- | --- |
| 2 本以上 | それらの平均スペクトル |
| 1 本 | そのスペクトル |
| なし | 範囲に最も近いスペクトル |

初期値のままでは、ほとんどの場合範囲内にスペクトルがないため、そのまま **OK** を
クリックすると、右クリックした保持時間に最も近いスペクトルが表示されます。ピーク
全体を含むように範囲を広げると、そのスペクトルを平均できます。

平均では、profile スペクトルは共通の m/z 軸に補間してから強度を平均します。centroid
スペクトルは、同じ m/z の強度を合計し、スペクトルの本数で割ります。

###  プリカーサーとプロダクトのスペクトル

MS/MS で測定したデータでは、スペクトルの右クリックメニューから、プリカーサー
スペクトルとそのプロダクトスペクトルの間を移動できます。

- **Precursor Spectrum**：表示中のスペクトルがプロダクトスペクトルのときに表示され
  ます。プリカーサースペクトルを描画します。
- **Product Spectrum ...**：表示中のスペクトルにプロダクトスペクトルがあるときに
  表示されます。ダイアログで 1 つ選ぶと、それを描画します。各項目は
  `Scan <number> (rt = <保持時間>, precursor=<プリカーサーの m/z>)` の形式で表示され
  ます。
