#  現在の制限事項

このページでは、Mass++4 でまだできないこと、または一部しかできないことをまとめています。
使い始める前に、このマニュアルのほかの章とあわせて確認してください。

##  データファイル

- **対応形式**：**File** -> **Open** -> **MS Data...** で開けるのは、mzML、Thermo
  `.raw`、Sciex `.wiff`、Shimadzu `.lcd` ファイルです。mzXML、MGF、Bruker `.d`
  などのほかの形式は直接開けません。ProteoWizard `msconvert` などで、先に mzML に
  変換してください。
- **ベンダーファイルには Docker が必要**：`.raw`、`.wiff`、`.lcd` ファイルは、Docker
  コンテナ内の ProteoWizard `msconvert` で mzML に変換されます。そのため、Docker
  Desktop をインストールして起動しておく必要があります。初回の変換では変換用の
  イメージをダウンロードするため、インターネット接続が必要で、時間もかかります。
  [インストール](01_Installation_ja.md#ベンダーの生データを開く場合)を参照してください。
- **変換メニュー**：**File** -> **Convert** -> **mzML...** が受け付けるのは `.raw` と
  `.wiff` ファイルだけです。`.lcd` ファイルは **File** -> **Open** ->
  **MS Data...** で開いてください。
- **同じファイルを 2 回開く**：すでに開いているファイルは、もう一度開けません。
- **サンプルを閉じる**：開いたサンプルは閉じられません。一覧から消すには Mass++4 を
  再起動してください。
- **データの保存**：Mass++4 は MS データファイルの保存や書き出しを行いません。
  保存できるのは、グラフの画像（PNG と SVG）とピークリスト（CSV とタブ区切り
  テキスト）です。

##  表示

- **ヒートマップと 3D 表示**：使われるのは MS スペクトル（MS Stage 1）だけで、
  保持時間を持つスペクトルが 2 本以上必要です。
  [mzML ファイルを開く](02_Opening_mzML_ja.md#ヒートマップ)を参照してください。
- **画像の書き出し**：PNG と SVG で保存できるのは、スペクトルとクロマトグラムだけ
  です。ヒートマップと 3D 表示からは保存できません。
  [SVG の書き出し](06_SVG_Export_ja.md)を参照してください。

##  ピーク検出

- **設定なし**：ピーク検出の方法は変更も調整もできません。
  [ピーク検出](05_Peak_Picking_ja.md)を参照してください。
- **ピークラベル**：位置は常に小数 3 桁で表示され、重なるラベルは表示されません。
- **API で送ったスペクトル**：ピーク検出は行われません。ラベルとピークリストに使わ
  れるのは、`io_add_annotation` で送ったピークだけです。そのため、アノテーションを
  送らない場合はラベルが表示されず、ピークリストも保存できません。

##  XIC と平均スペクトル

- **XIC**：使われるのは MS スペクトルだけです。m/z の範囲は、ppm ではなく m/z の
  絶対値の許容幅で指定します。
  [TIC、XIC とスペクトル間の移動](03_TIC_XIC_and_Spectrum_Navigation_ja.md#xic)を
  参照してください。
- **centroid データの平均スペクトル**：まとめられるのは、m/z が完全に一致する
  ピークだけです。スペクトルごとに m/z がわずかに異なるピークは、別々のピークの
  まま残ります。

##  Peak Filter と Mass Calculator

- **検索対象**：Peak Filter が検索するのは MS/MS スペクトルだけです。
  [具体的な組成式を使ったピークフィルタ](04_Peak_Filtering_with_one_concrete_formula_ja.md)
  を参照してください。
- **許容幅**：m/z の許容幅は、ppm ではなく m/z の絶対値です。
- **Neutral Loss**：**Neutral Loss** の設定はピークと一緒に保存されますが、まだ検索
  には使われません。
- **Mass Calculator のイオン**：計算できるのは正イオンだけです（`[M+H]+`、
  `[M+2H]+`、`[M+3H]+`、`[M+Na]+`、`[M+2Na]+`、`[M+3Na]+`、`[M+X]+`）。

##  Python API

- **認証なし**：コンピュータのポート `8191` に接続できれば、ネットワーク上のほかの
  コンピュータを含め、誰でも API を使えます。
  [Python との MS データ連携の例](07_Python_MS_data_link_example_ja.md#必要なもの)を
  参照してください。
- **ポートは固定**：API は常にポート `8191` を使います。
- **選択中のサンプルだけ**：`io_get_spectra_count`、`io_get_current_index`、
  `io_get_spectrum` は、**Sample** テーブルで選択しているサンプルを対象に動きます。
- **エラーメッセージ**：サンプルが選択されていないなどでリクエストが失敗すると、
  API はエラーメッセージではなく空の応答を返します。
- **アノテーション**：`io_add_annotation` で表示されるのは構造の画像だけで、
  アノテーションの `name` は表示されません。
