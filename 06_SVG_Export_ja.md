#  SVG の書き出し

スペクトルやクロマトグラムは、SVG ファイルとして保存できます。SVG はベクター形式な
ので、どの大きさでも図がくっきり表示され、Inkscape や Adobe Illustrator などの作図
ソフトで編集できます。論文、ポスター、スライドの図に使ってください。

この章は、[mzML ファイルを開く](02_Opening_mzML_ja.md)の手順でスペクトルまたは
クロマトグラムを描画していることを前提にしています。

##  グラフを SVG で保存する

1. 保存したい状態でグラフを表示します。スペクトルまたはクロマトグラムを選び、必要な
   範囲に拡大してください。
2. グラフを右クリックし、**Save as** -> **SVG...** を選びます。

   ![Save as > SVG...](images/screenshots/06-01-save_svg_menu.png)

3. **Save SVG File** ダイアログでフォルダを選び、`.svg` で終わるファイル名を入力して
   `Save`（保存）をクリックします。

##  保存される内容

SVG ファイルには、グラフが現在の表示のとおりに保存されます。

| 項目 | SVG ファイルでの扱い |
| --- | --- |
| 表示範囲 | データ全体ではなく、拡大している範囲 |
| 波形 | 多数の短い線分で描画 |
| 軸、目盛りの数値、軸の名前 | 含まれます。文字は等幅フォントの文字データとして残ります |
| ピークラベル | 画面に表示されているとおりに含まれます |
| Peak Filter の目印 | 画面に表示されている場合に含まれます |
| サイズ | 画面上のグラフの幅と高さ（ピクセル） |
| 背景 | 透明 |

ファイルのサイズは画面上のグラフの大きさで決まるので、図の縦横比を変えたいときは、
保存する前に Mass++4 のウィンドウの大きさを変えてください。サイズは、作図ソフトで
後から変えても画質は落ちません。

背景は透明です。色の付いた背景の上に図を置く場合は、必要に応じて作図ソフトで図の
後ろに白い四角を置いてください。

##  SVG ファイルの編集

- 目盛りの数値、軸の名前、ピークラベルは文字データなので、作図ソフトでフォント、
  大きさ、内容を変えられます。
- 波形は多数の線分でできています。色や線の太さをまとめて変えるには、グループ化など
  で線分をまとめて選択してください。

##  SVG と PNG の違い

同じ **Save as** メニューには **PNG...** もあります。

| | SVG | PNG |
| --- | --- | --- |
| 形式 | ベクター | ビットマップ |
| 拡大 | くっきりしたまま | ぼやける |
| 文字や線の編集 | 作図ソフトで可能 | 不可 |
| 主な用途 | 論文、ポスター、編集する図 | 手軽な共有、文書、ウェブページ |
